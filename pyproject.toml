[project]
name = "python-foundations"
version = "0.1.0"
description = "A modern Python learning laboratory focused on fundamentals, APIs, automation, and AI."
readme = "README.md"
authors = [
    { name = "Facundo Sevrini", email = "52105765+cacu88@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python-foundations = "python_foundations:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
